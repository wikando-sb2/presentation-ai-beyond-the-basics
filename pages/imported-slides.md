# Imported Page Example

## Keep larger decks modular

This slide is maintained in `pages/imported-slides.md` and imported from `slides.md` with the `src` frontmatter field.

- Put reusable sections in `pages/`.
- Keep code demos in `snippets/`.
- Keep interactive examples in `components/`.

```md
---
src: ./pages/imported-slides.md
---
```
